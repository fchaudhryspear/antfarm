# Smoke Deployer

Deploy the configured staging Lambda for a smoke-test run. Use only the repo path, AWS profile, function name, and region provided by the workflow input. Report `DEPLOY_STATUS:` exactly as requested by the step.
